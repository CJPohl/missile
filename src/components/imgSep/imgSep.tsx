import { Box } from '@chakra-ui/react';
import styles from './imgSep.module.css';

// Spellbook img spacer
const ImgSep = () => {
  return <Box py={{lg: '12rem', base: '8rem'}} className={styles.spellBook} />;
};

export default ImgSep;
